#include "circleFinder.h"
#include <stdio.h>



int main(int argc, char** argv){
	CircleFinder finder;

	vector<ball> result;
	/*if(argc > 1)
		finder.getLocations(argv[1], atoi(argv[2]), result);
	else
    	finder.getLocations("color-test-2.pnm", 2, result);
	*/
	return 0;
}
/*
balls  position( 0.312907 , 0.500285 )
balls  position( 0.667113 , 0.674908 )
balls  position( 0.811136 , 0.469727 )
*/
